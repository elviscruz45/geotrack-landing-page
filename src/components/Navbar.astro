---
const navItems = [
  { name: "Caracteristicas", href: "#" },
  { name: "Equipo", href: "#" },
  { name: "Contactanos", href: "#" },
];
---

<nav>
  <ul class="item-center flex justify-start gap-8">
    {
      navItems.map((item) => (
        <li>
          <a
            class="text-accent-400 transition-colors duration-300 hover:text-white"
            href={item.href}
          >
            {item.name}
          </a>
        </li>
      ))
    }
  </ul>
</nav>
